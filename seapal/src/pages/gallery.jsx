import Filter from "../component/filter";
export default function Gallery() {
  return <Filter />;
}
/* make a grid of four columns don't set the rows ones the filter button is clicked give the first column to the dropdown list of filters*/
/*we should have another sidebar and navbar only for this page (not sure about the navbar??)*/